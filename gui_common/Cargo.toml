[package]
name = "gui_common"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.33"
midi_harness = { path = "../midi_harness" }

[dev-dependencies]
egui_kittest = { version = "0.33", features = ["wgpu", "snapshot"] }
